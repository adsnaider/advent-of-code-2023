use std::collections::HashMap;

use day3::Grid;
use util::read_full_input;

fn main() {
    let grid: Grid = read_full_input().parse().unwrap();
    let mut sum = 0;

    let mut gear_adjecensy = HashMap::new();

    println!("{sum}");
}
